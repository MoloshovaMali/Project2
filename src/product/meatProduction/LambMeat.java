package product.meatProduction;

import java.time.LocalDate;

public class LambMeat extends MeatProduction {
    public LambMeat(int id, String name, String companyName, LocalDate date) {
        super(id, name, companyName, date);
    }
}
